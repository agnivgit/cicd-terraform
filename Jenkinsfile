pipeline {
    agent any
    stages {
        stage('Run 'terraform init'') {
            steps {
            sh '''
                terraform init
                '''
              }
            }    
        }    
    }
